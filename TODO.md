# TODO - Deployment Fix (Not Found)

- [x] Update `Dockerfile` to correctly copy `backend/` into `/app/backend`.
- [x] Ensure frontend build is copied to `/app/frontend/build`.
- [x] Change container start command to `node backend/server.js`.
- [x] Rebuild Docker image and redeploy to Railway.

- [ ] Verify that React routes return `index.html` and `/api/*` routes work.


